import React from 'react';
import './Loader.css';

const Loader = () => {
  return (
    <div className="loading">
      <span>Loading...</span>
    </div>
  );
};

export default Loader;
